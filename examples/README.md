# Examples

No copyrighted audio is included. Put your own local audio file here or run:

```bash
kpop-scope analyze /path/to/song.mp3 --output outputs/song --plots
```

`demo_analysis_schema.json` shows the rough output shape.
